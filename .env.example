# ANIMEHUB PROJECT ENVIRONMENT VARIABLES (Template for .env file)

# -----------------------------------------------------------------
# 1. DATABASE CONFIGURATION (Used by the .NET API for EF Core)
# -----------------------------------------------------------------
# NOTE: This line is used by the .NET API if configured to read .env
# for a database connection string. The actual connection string is
# usually also stored in appsettings.Development.json in the API project.
# Use a secure password for your local .env file!
DB_CONNECTION_STRING="Server=localhost;Database=AnimeHubDB;User Id=DevUser;Password=MySecretPassword;"


# -----------------------------------------------------------------
# 2. FRONTEND CLIENT CONFIGURATION (Vite/React)
# -----------------------------------------------------------------
# VITE requires variables to be prefixed with VITE_ to be exposed to the client.

# Base URL for the ASP.NET Core API running locally (https://localhost:7001)
# Note: Ensure this matches the port your .NET Kestrel server runs on.
VITE_API_BASE_URL="https://localhost:57088"

# A placeholder for your initial authentication or API key if needed later.
VITE_AUTH_SECRET="A_Placeholder_Client_Secret"